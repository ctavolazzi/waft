# Work Effort: WAFT Control Center Unification

## Status: Completed
**Started:** 2026-03-14 13:51 UTC
**Last Updated:** 2026-03-14 14:20 UTC

## Objective
Create a unified WAFT browser interface by making the Svelte visualizer served by `waft serve` the canonical control center, folding the existing 5050 orchestration context into it, and replacing dead or fragmented navigation with real workspace links.

## Tasks
1. [x] Audit the existing UI surfaces and select the canonical path.
2. [x] Create this work effort and point older UI efforts at it.
3. [x] Implement a unified control-center experience in the visualizer.
4. [x] Add focused backend and frontend checks for the new unified surface.
5. [x] Run manual UI validation and record artifacts.

## Progress
- Confirmed `waft serve` + FastAPI + Svelte visualizer is the safest canonical UI path.
- Confirmed the older Streamlit dashboard is useful history but not the best current integration point.
- Confirmed the 5050 backend APIs already exist and can be folded into the visualizer.
- Identified dead navbar links (`/git`, `/work-efforts`, `/empirica`) as a concrete unification bug to fix.
- Turned the visualizer home page into a WAFT Control Center with unified workspace cards, 5050 context stack, recent timeline, and recent devlog sections.
- Fixed the `/api/projects/stats` route-ordering bug so the Projects workspace loads successfully from the unified nav.
- Fixed three pre-existing Svelte parser blockers (`ResourcePanel.svelte`, `RunDetails.svelte`, `DataTable.svelte`) so the Flow Lab and related visualizer surfaces are demoable in dev mode.

## Development Plan
1. Extend the shared frontend API client with typed 5050 session/timeline calls.
2. Add a small 5050 store for read-only control-center data.
3. Turn the visualizer home page into a true WAFT control center:
   - existing project state summary
   - real workspace navigation cards
   - 5050 session snapshot and recent timeline
4. Replace dead navbar links with real, supported destinations.
5. Add targeted API tests and visualizer checks, then validate the UI in-browser.

## Verification
1. `uv run pytest tests/api/test_projects_basic.py tests/api/test_dashboard_5050.py -q`
2. `uv run ruff check src/waft/api/routes/dashboard_5050.py tests/api/test_dashboard_5050.py tests/api/test_projects_basic.py`
3. `npm run check` (warning: still blocked by substantial pre-existing visualizer type debt outside this change)
4. `npm run build` (warning: still blocked by additional pre-existing visualizer build issues outside the unified UI slice)
5. Manual browser validation on the dev path:
   - `uv run waft serve --dev --host 0.0.0.0 --port 8000`
   - `npm run dev -- --host 0.0.0.0 --port 8781`
   - verified Control Center, Projects, and Flow Lab interactively

## Next Steps
1. Optional follow-up: continue paying down visualizer-wide Svelte/type debt until `npm run check` and `npm run build` go green.
2. Optional follow-up: add availability/status metadata per workspace so navigation can advertise “stable” vs “experimental”.

## Notes
- Canonical successor for prior UI efforts:
  - `WE-260112-yfdi` Streamlit dashboard effort
  - `WE-260301-5050` localhost:5050 dashboard history in `devlog.md`

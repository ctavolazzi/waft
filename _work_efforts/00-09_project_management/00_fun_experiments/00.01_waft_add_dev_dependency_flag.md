# Work Effort: Make `waft add --dev` real

## Status: Completed
**Started:** 2026-03-14 13:23 UTC
**Last Updated:** 2026-03-14 13:33 UTC

## Objective
Fix the `waft add --dev` command so it actually forwards `--dev` to `uv add` instead of warning and adding a regular dependency.

## Tasks
1. [x] Inspect the current CLI and substrate implementation.
2. [x] Create a development plan and log the starting context.
3. [x] Implement `--dev` forwarding in the substrate and CLI layers.
4. [x] Add focused tests for command forwarding and CLI behavior.
5. [x] Run targeted verification, then record the outcome.

## Progress
- Confirmed the CLI advertises `--dev` support but explicitly ignores it.
- Confirmed `SubstrateManager.add_dependency()` only constructs `uv add <package>`.
- Confirmed `empirica` is unavailable on PATH in this environment, so local work-effort docs are the reliable session record.
- Updated `waft add` to forward `dev=True` into the substrate layer and print the real command being run.
- Added deterministic regression coverage for both plain and `--dev` substrate command construction plus CLI flag forwarding.
- Verified the change with targeted pytest coverage and test-file lint checks.

## Development Plan
1. Extend `SubstrateManager.add_dependency()` with a `dev` flag.
2. Remove the misleading warning path from `waft add`.
3. Add deterministic tests that assert command construction and CLI forwarding.
4. Run only the targeted test files needed for this change.

## Verification
1. `uv run pytest tests/test_substrate.py -k "add_dependency_builds" -q`
2. `uv run pytest tests/test_commands.py -k "waft_add_forwards_dev_flag" -q`
3. `uv run ruff check tests/test_substrate.py tests/test_commands.py`

## Next Steps
1. No immediate follow-up required for this fix.
2. Optional future cleanup: address pre-existing `ruff` findings in `src/waft/main.py`.

## Notes
- This is intentionally a small “fun” improvement: compact scope, clear user value, and clean testability.

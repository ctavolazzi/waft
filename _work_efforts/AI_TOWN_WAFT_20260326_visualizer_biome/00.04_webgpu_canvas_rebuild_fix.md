# Work Effort: WebGPU canvas rebuild fix

## Status: Completed
**Started:** 2026-03-27 06:05 PDT
**Last Updated:** 2026-03-27 06:49 PDT

## Objective
Resolve biome WebGPU runtime failure (`Cannot read properties of null (reading 'configure')`) when toggling renderer mode.

## Tasks
1. [x] Inspect biome route rebuild lifecycle and WebGPU engine mount path
2. [x] Add safe canvas remount behavior on renderer-mode switch
3. [x] Prevent stale async rebuild races from leaving bad engine state
4. [x] Run diagnostics on touched file

## Progress
- Patched `visualizer/src/routes/biome/+page.svelte`:
  - Added keyed canvas remount with `{#key state.rendering.useWebGPU}` so WebGL/WebGPU do not reuse the same canvas context.
  - Added `await tick()` in `rebuild()` to wait for canvas remount before engine construction.
  - Added rebuild version guard to avoid stale async rebuild races.
- Patched `visualizer/src/lib/biome/engine-webgpu.ts`:
  - Added missing `MeshBasicMaterial` import from `three/webgpu` to fix `ReferenceError` during volumetric overlay creation in `ensureVolumetrics()`.
- Patched `visualizer/src/routes/biome/+page.svelte`:
  - Added one-button extras toggle (`Disable extras` / `Enable extras`) for debug workflow.
  - Toggle now flips optional visuals as a bundle (fog, rain, rain debug overlays, sky, caustics, volumetrics).
  - Added snapshot/restore behavior: disabling extras stores current states; enabling restores previous states.
- Refined `visualizer/src/routes/biome/+page.svelte` extras toggle:
  - Added renderer mode to bundle (`rendering.useWebGPU`) so debug-minimal mode can force WebGL.
  - `Disable extras` now sets `useWebGPU: false` alongside optional effects off.
  - `Enable extras` restores prior renderer mode and effect flags from snapshot.
- Reduced runtime console noise for normal use:
  - Updated `visualizer/src/routes/biome/+page.svelte` UI lifecycle logs (`mount`, `rebuild-start`, `rebuild-done`) to be opt-in via URL query `?debugBiome=1`.
  - Updated `visualizer/src/lib/biome/engine-webgpu.ts` periodic telemetry logs to same opt-in `?debugBiome=1` flag.
  - Switched debug output from warn-level to info-level for cleaner default console.
- Fixed debug logging ergonomics after user validation:
  - Restored warn-level debug logs for visibility when debug mode is enabled.
  - Made debug flag parsing tolerant of malformed query strings (e.g. `?=debugBiome=1`) by matching raw search text.
  - Kept logs disabled by default without `debugBiome=1`.
- Tuned WebGPU volumetric composite to prevent whiteout:
  - Reduced scatter contribution and banding gain in `updateVolumetricTexture()`.
  - Reduced final light scaling and RGB multipliers.
  - Lowered volumetric alpha cap significantly to keep terrain/sky readable.
- Tuned WebGL sky path for whiteout cases:
  - Lowered WebGL renderer exposure (`toneMappingExposure`) in `engine.ts`.
  - Set explicit sky scattering uniforms (`mieCoefficient`, `mieDirectionalG`) in `syncSky()` to avoid over-bright sky dome output.
- Reverted speculative visual tuning to establish a clean baseline:
  - Restored original WebGL sky and exposure behavior in `engine.ts`.
  - Restored original WebGPU volumetric composition in `engine-webgpu.ts`.
- Reduced telemetry noise to rebuild-time state snapshots only:
  - Added state bundle logs in `+page.svelte` rebuild start/done (`useWebGPU`, `volumetrics`, `sky`, `caustics`, `waterFog`).
  - Removed per-frame WebGPU runtime/volumetric debug spam.
- Current diagnosis from user evidence:
  - Whiteout most consistently appears on WebGL path (no `webgpu-engine-constructed` log present in whiteout runs).
  - Dark/usable view appears when WebGPU path is active in provided repro set.
- Validation:
  - `ReadLints` for touched file: only existing warning (`params` export style).
  - `ReadLints` for `engine-webgpu.ts`: no diagnostics.
  - `npm run check` in `visualizer`: still fails due to pre-existing unrelated TypeScript diagnostics across existing files.

## Next Steps
1. Manual verification in browser: enable WebGPU + volumetrics at `/biome` and confirm no `MeshBasicMaterial` runtime crash.
2. Optional cleanup pass for unrelated `visualizer` typecheck backlog.

## Notes
- Root cause is consistent with context reuse conflict: WebGPU initialization on a canvas previously used by WebGL can produce a null WebGPU context in some runtimes.

#import "@preview/aero-check:0.1.1": *

#show: checklist.with(
  title: "Perfect Sourdough Bread Recipe",
  disclaimer: "Follow steps in order. Timing is critical for best results.",
  style: 0,
)

#topic("Preparation (Day Before)")[
  #section("Starter")[
    #step("Feed sourdough starter 8-12 hours before", "Check")
    #step("Starter should double in volume", "Check")
    #step("Starter should pass float test", "Check")
  ]
]

#topic("Mixing & Autolyse")[
  #section("Initial Mix")[
    #step("Weigh 500g bread flour", "Check")
    #step("Weigh 350g warm water (70°F)", "Check")
    #step("Mix until no dry flour remains", "Check")
    #step("Rest 30-60 minutes (autolyse)", "Check")
  ]
]

#colbreak()

#topic("Bulk Fermentation")[
  #section("First Mix")[
    #step("Add 100g active starter", "Check")
    #step("Add 10g salt", "Check")
    #step("Mix until well combined", "Check")
    #step("Perform 4 sets of stretch & folds (30 min apart)", "Check")
    #step("Bulk ferment 3-4 hours at 75°F", "Check")
  ]
]

#topic("Shaping & Proofing")[
  #section("Final Steps")[
    #step("Pre-shape and rest 20 minutes", "Check")
    #step("Final shape and place in banneton", "Check")
    #step("Cold proof in refrigerator 12-24 hours", "Check")
    #step("Preheat Dutch oven to 500°F", "Check")
    #step("Score dough and bake 20 min covered", "Check")
    #step("Bake 20-25 min uncovered at 450°F", "Check")
  ]
]

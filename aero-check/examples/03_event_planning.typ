#import "@preview/aero-check:0.1.1": *

#show: checklist.with(
  title: "Conference Event Planning Checklist",
  disclaimer: "Complete all items to ensure successful event execution.",
  style: 0,
)

#topic("6 Months Before")[
  #section("Planning")[
    #step("Secure venue and date", "Check")
    #step("Define event budget", "Check")
    #step("Create event website/landing page", "Check")
    #step("Design marketing materials", "Check")
    #step("Set ticket pricing structure", "Check")
  ]
]

#topic("3 Months Before")[
  #section("Content & Speakers")[
    #step("Confirm keynote speakers", "Check")
    #step("Finalize session schedule", "Check")
    #step("Prepare speaker contracts", "Check")
    #step("Order event signage and banners", "Check")
    #step("Plan catering menu", "Check")
  ]
]

#colbreak()

#topic("1 Month Before")[
  #section("Logistics")[
    #step("Send speaker information packets", "Check")
    #step("Confirm AV equipment rental", "Check")
    #step("Arrange transportation for speakers", "Check")
    #step("Finalize attendee count", "Check")
    #step("Prepare registration materials", "Check")
  ]
]

#topic("1 Week Before")[
  #section("Final Preparations")[
    #step("Conduct venue walkthrough", "Check")
    #step("Test all AV equipment", "Check")
    #step("Prepare welcome packets", "Check")
    #step("Brief volunteer staff", "Check")
    #step("Confirm catering final numbers", "Check")
  ]
]

#topic("Day Of Event")[
  #section("Setup")[
    #step("Arrive 3 hours early", "Check")
    #step("Set up registration desk", "Check")
    #step("Test microphones and projectors", "Check")
    #step("Display directional signage", "Check")
    #step("Prepare speaker green room", "Check")
  ]
]

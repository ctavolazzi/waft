#import "@preview/aero-check:0.1.1": *

#show: checklist.with(
  title: "Learning New Technology Checklist",
  disclaimer: "Follow systematically. Practice is essential. Build projects.",
  style: 1,
)

#topic("Foundation Phase")[
  #section("Getting Started")[
    #step("Identify learning resources (books, courses, docs)", "Check")
    #step("Set up development environment", "Check")
    #step("Complete 'Hello World' tutorial", "Check")
    #step("Understand core concepts and syntax", "Check")
    #step("Join relevant community forums", "Check")
  ]
]

#topic("Practice Phase")[
  #section("Hands-On Learning")[
    #step("Complete beginner exercises", "Check")
    #step("Build small practice projects", "Check")
    #step("Read and understand example code", "Check")
    #step("Experiment with variations", "Check")
    #step("Solve coding challenges", "Check")
  ]
]

#colbreak()

#topic("Application Phase")[
  #section("Real Projects")[
    #step("Identify real-world problem to solve", "Check")
    #step("Design solution architecture", "Check")
    #step("Implement core features", "Check")
    #step("Test and debug thoroughly", "Check")
    #step("Refactor for best practices", "Check")
  ]
]

#topic("Mastery Phase")[
  #section("Advanced Learning")[
    #step("Study advanced patterns and techniques", "Check")
    #step("Contribute to open source projects", "Check")
    #step("Teach others (blog, video, or mentor)", "Check")
    #step("Stay updated with latest developments", "Check")
    #step("Build portfolio of projects", "Check")
  ]
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cognitive-prosthetics-cli"
version = "0.1.0"
description = "Deterministic readiness checks for cognitive prosthetics workflows."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cprost = "cognitive_prosthetics_cli.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

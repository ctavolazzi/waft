Open and refresh the localhost:5050 dashboard workflow for the current WAFT project.

Steps:
1. Ensure the WAFT API/dashboard server is running on localhost:5050 with:
   `waft dashboard-5050 --port 5050 --host localhost --open-browser`
2. If the server is already running, do not start another instance.
3. Confirm dashboard is reachable at:
   `http://localhost:5050`
4. Confirm API health:
   `http://localhost:5050/api/health`
5. Return a short status summary and the next recommended command bundle for continuation.

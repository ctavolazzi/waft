[project]
name = "internal-monologue"
version = "0.1.0"
description = "NarcissusAgent 3-Body Architecture with Console Goblin"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40.0",
]

[project.scripts]
homebase = "theguide_hello:main"

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-playwright>=0.4.0",
]

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Playwright settings
addopts = --browser chromium --headed

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    sse: marks tests that require SSE connection
